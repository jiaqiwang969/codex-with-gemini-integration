# AI 使用 Hunyuan MCP 的正确方法

## ⚠️ 重要：处理剪贴板图片的标准流程

当用户粘贴图片时（显示为 `[codex-clipboard-xxx.png 宽x高]`），你应该：

### ✅ 正确做法
```javascript
// 不要传递 image_url 参数！系统会自动处理
hunyuan-3d.hunyuan_generate_3d({
  "api_version": "Rapid",
  "enable_pbr": true
  // 注意：不要传 image_url！
})
```

### ❌ 错误做法
1. ❌ 不要传递 image_url 参数（尤其是长 data URL）
2. ❌ 不要尝试搜索文件位置
3. ❌ 不要使用 mdfind、find 或 ls 查找文件
4. ❌ 不要要求用户提供文件路径

## 📝 示例对话

### 用户输入：
```
[codex-clipboard-xH9LDc.png 372x366]
帮我将这个图片通过hunyuan大模型，转成3d模型
```

### 你应该立即调用：
```javascript
hunyuan-3d.hunyuan_generate_3d({
  "api_version": "Rapid",
  "enable_pbr": true,
  "face_count": 120000
  // 重要：不传 image_url，不传 prompt！
  // 系统会自动注入图片
})
```

## 🔧 系统会自动处理

1. MCP Handler 会自动从会话中提取 data URL
2. 无需传递任何图片参数
3. 避免在界面显示长 base64 字符串
4. 系统会智能判断并注入图片数据

## ⚠️ 重要限制

1. **image_url 和 image_base64 不能同时存在！**
   - 只能选择其中一个参数
   - 如果同时传递会返回错误

2. **prompt 不能与 image_url/image_base64 同时存在！**
   - 纯文本生成：只用 prompt
   - 图片生成：只用 image_url 或 image_base64，不要传 prompt
   - 这是腾讯云 API 的硬性限制

## ⚡ 快速检查清单

看到 `[codex-clipboard-xxx.png]` → 立即调用 MCP 工具
- ✅ 不传递 `image_url` 参数（系统自动处理）
- ✅ 不传递 `prompt` 参数（图片模式不需要）
- ❌ 不要传递：`"codex-clipboard-xxx.png"`
- ❌ 不要传递：`"[剪贴板图片]"` 等占位符
- ❌ 不要传递：data URL (base64 编码)
- ❌ 不要传递：prompt 文本描述（API 限制）
- ❌ 不要同时传递多个图片参数
