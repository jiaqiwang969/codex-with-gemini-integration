# AI 使用 Hunyuan MCP 的正确方法

## ⚠️ 重要：处理剪贴板图片的标准流程

当用户粘贴图片时（显示为 `[codex-clipboard-xxx.png 宽x高]`），你应该：

### ✅ 正确做法
```javascript
// 直接调用 MCP 工具，只传递文件名
hunyuan-3d.hunyuan_generate_3d({
  "image_url": "codex-clipboard-xxx.png",  // 只传文件名！
  "prompt": "用户的描述（如果有）"
})
```

### ❌ 错误做法
1. ❌ 不要尝试搜索文件位置
2. ❌ 不要使用 mdfind、find 或 ls 查找文件
3. ❌ 不要要求用户提供文件路径
4. ❌ 不要加任何路径前缀

## 📝 示例对话

### 用户输入：
```
[codex-clipboard-xH9LDc.png 372x366]
帮我将这个图片通过hunyuan大模型，转成3d模型
```

### 你应该立即调用：
```javascript
hunyuan-3d.hunyuan_generate_3d({
  "image_url": "codex-clipboard-xH9LDc.png",
  "api_version": "Rapid",
  "enable_pbr": true,
  "face_count": 120000
})
```

## 🔧 系统会自动处理

1. MCP Handler 会自动找到文件的实际位置
2. 文件总是在系统临时目录（`$TMPDIR`）
3. 你不需要知道具体路径
4. 系统会自动处理路径解析

## ⚡ 快速检查清单

看到 `[codex-clipboard-xxx.png]` → 立即调用 MCP 工具
- ✅ 传递：`"codex-clipboard-xxx.png"`
- ❌ 不要传递：`"/path/to/codex-clipboard-xxx.png"`
- ❌ 不要传递：`"$TMPDIR/codex-clipboard-xxx.png"`
